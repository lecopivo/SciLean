import SciLean.Data.Compressed
import SciLean.Mathlib.Data.Table

namespace SciLean

-- def ConstantCompression (α γ ι : Type) [Table α ι γ] [DecidableEq γ] [Enumtype ι] : Compression α γ where
--   canCompress a := Id.run do
--     let mut r : Bool := true
--     let i : ι := sorry
--     let x0 := a[i]
--     for i in ι do
--       r := r && (
--     r
--   uncompress p := 0
--   compress a := ()

--   valid := sorry
--   unique := sorry

